# Env var names to write into .env for Terraform (one per line).
# Values are taken from the environment (e.g. loaded by 1Password or workflow secrets).
# Downstream repos: add more names here and set those vars in the workflow (secrets/1Password).
GITHUB_PAT
