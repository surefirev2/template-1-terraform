# Local .env template — copy to .env and fill in (never commit .env).

# S3 remote state (Terraform AWS provider credential chain)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1

# GitHub provider (optional if you use github_* resources)
GITHUB_PAT=

# Other
ANTHROPIC_API_KEY=
